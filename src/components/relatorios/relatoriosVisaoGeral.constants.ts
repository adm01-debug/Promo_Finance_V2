export const RELATORIOS_CORES = [
  'hsl(var(--primary))',
  'hsl(var(--secondary))',
  'hsl(var(--success))',
  'hsl(var(--accent))',
  'hsl(var(--warning))',
  'hsl(var(--destructive))',
];

export const ESTILO_TOOLTIP_RELATORIOS = {
  backgroundColor: 'var(--bg-2)',
  backdropFilter: 'blur(10px)',
  border: '1px solid var(--line)',
  borderRadius: '16px',
  color: 'var(--t0)',
  fontWeight: 'bold',
  fontSize: '12px',
};
